# Generated by: cmenvrc migrate
# Review and adjust before committing.
# This file contains NO secret values — safe to commit to git.

project: orchstep

# Common: shared across all environments
common:
  secrets:
    # Format: ENV_VAR_NAME: bitwarden-reference-name
    NPM_TOKEN: npm-token--orchstep
    PYPI_TOKEN: pypi-token--orchstep

# Per-environment profiles
# Extend with staging, prod, etc. Profile values override common.
profiles:
  dev:
    vars:
      # Example: override common vars for dev
      # LOG_LEVEL: debug
    # secrets:
      # Example: per-env secret overrides
      # NPM_TOKEN: npm-token--orchstep--dev
      # PYPI_TOKEN: pypi-token--orchstep--dev
